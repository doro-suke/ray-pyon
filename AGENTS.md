# レイぴょん (シフト自動作成アプリ) - AIエージェント開発ルール

## 1. プロジェクト概要
- **アプリ名**: レイぴょん（シフト自動作成）
- **目的**: 病院・施設などのスタッフの希望休や各種制約条件を満たすシフト表を自動的に生成するWebアプリケーション。
- **技術スタック**:
  - UI/フレームワーク: Python / Streamlit
  - 最適化・ロジック: Google OR-Tools (CP-SAT solver)
  - データ処理: Pandas
  - その他: jpholiday（祝日判定）, streamlit-local-storage（ブラウザ保存）

## 2. 開発・コーディングの基本原則
- **単一ファイル構成**: 現在、UIと最適化ロジックは `app.py` に集約されています。極端に巨大化しない限り、単一ファイルのまま保守性を高める方向で進めてください（複雑になりすぎる場合のみモジュール分割を提案してください）。
- **コード品質**: PEP8に準拠し、可読性の高いコードを書くこと。複雑なロジック（特にOR-Toolsの制約部分）には、意図を説明する日本語のコメントを必ず残すこと。
- **実証主義**: 変更を加えた後は、必ずStreamlitアプリとしてエラーなく動作するかどうか（必要であればPowerShell上で `streamlit run app.py` を用いた起動テストなど）を確認すること。

## 3. シフト最適化ロジック (OR-Tools) の変更ルール
最適化エンジン部分（`create_shift_schedule` 関数内）を変更する際は、以下の点に注意してください。
- **ハード制約とソフト制約の区別**:
  - **ハード制約 (Hard Constraints)**: 「当直は1日1人まで」「連続勤務日数の上限」など、**絶対に破ってはいけないルール**。`model.Add(...)` を使って厳格に定義します。
  - **ソフト制約 (Soft Constraints)**: 「スタッフの希望休」「当直回数の公平性」など、**なるべく満たしたい（破った場合のペナルティを最小化する）ルール**。ペナルティ変数を定義し、`model.Minimize(...)` の対象に追加します。
- 制約を追加・変更した際は、「実行不能 (INFEASIBLE)」に陥りやすいため、緩めの条件でも解が見つかるか（フェイルセーフ）を意識して設計してください。

## 4. UI/状態管理の変更ルール (Streamlit)
- **状態の保持**: Streamlit特有のリロードによるデータリセットを防ぐため、UI入力値の保存には `st.session_state` または `streamlit-local-storage` (`get_state`, `save_state`) を適切に使用すること。
- **ユーザー体験**: エラー発生時（制約が厳しすぎてシフトが組めない場合など）は、ユーザーに原因（例: 「特定の日の希望休が多すぎます」等）を分かりやすく `st.error()` や `st.warning()` でフィードバックすること。

## 5. 絶対禁止事項
- このプロジェクトにおいて、外部のデータベースへの接続や外部API（有料APIなど）への依存を追加することは禁止します（完全ローカル完結・フォルダ内完結を維持するため）。
- （過去の別プロジェクトの名残である）SPRTテストや不必要なロギング、複雑な5フェーズ運用などの過剰なMLOpsの導入は行わないでください。
